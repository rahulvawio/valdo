apiVersion: v1
kind: Service
metadata:
  name: pots-service
  labels:
    name: clust-in
    igns: nbjs
spec:
  type: NodePort
  selector:
    name: nginx
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80