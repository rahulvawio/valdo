apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: plox
spec:
  containers:
  - name: php-redis22
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
